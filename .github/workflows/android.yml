name: Android CI

on:
  push:
    branches: [ "master-ci/cd" ]
  pull_request:
    branches: [ "master-ci/cd" ]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4

    - name: Run a one-line script
      run: echo Hello, World!
    - name: Run multi-line script
      run: echo add actions to build
