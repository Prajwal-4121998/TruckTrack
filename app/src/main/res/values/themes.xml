<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.TruckTrack" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <item name="android:windowBackground">@color/dark_teal_gray</item>
        <item name="android:includeFontPadding">false</item>

        <!-- Status bar color -->
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:windowTranslucentStatus">false</item>
    </style>

    <style name="Theme.TruckTrack" parent="Base.Theme.TruckTrack" />

    <style name="Theme.App.Starting" parent="Theme.SplashScreen">
        <item name="android:windowBackground">@color/dark_teal_gray</item>
        <item name="windowSplashScreenBackground">@color/dark_teal_gray</item>
        <item name="windowSplashScreenIconBackgroundColor">@android:color/transparent</item>
        <item name="postSplashScreenTheme">@style/Theme.TruckTrack</item>
    </style>

    <style name="CustomTheme" parent="Base.Theme.TruckTrack">
        <item name="android:textSelectHandleLeft">@drawable/zero_px_transparent</item>
        <item name="android:textSelectHandleRight">@drawable/zero_px_transparent</item>
        <item name="android:textSelectHandle">@drawable/zero_px_transparent</item>
    </style>
</resources>